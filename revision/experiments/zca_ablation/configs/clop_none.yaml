# ZCA ablation — condition: none (raw embeddings, no preprocessing)
# Same as production except use_preprocessed=false and save_dir.

cache_dir: "data/cached_latents"
use_preprocessed: false
use_deduplicated: true

seed: 42
variant_prob: 0.0
val_split: 0.1
n_folds: 1
fold_idx: 0
num_workers: 4
split_strategy: "stratified"

# Model — adjusted for raw embedding dimensions (text=1024, cell=512)
text_dim: 1024
cell_dim: 512
proj_dim: 512
text_layers: 3
cell_layers: 3
dropout: 0.2
temperature: 14.0
temperature_init: 14.0
temperature_learnable: false
label_smoothing: 0.1
use_batch_norm: false
use_ema: false
cell_noise_std: 0.0
use_whitening: false

# Loss
loss_type: "prototype_siglip"
auto_duplicate_mask: true
cohesion_weight: 0.15
max_temperature: 16.0
temp_reg_weight: 0.0
separation_margin: 0.2
separation_threshold: 0.2

# Training
batch_size: 1024
lr: 5.0e-4
weight_decay: 0.06
num_epochs: 100
warmup_epochs: 5
grad_clip: 1.0
use_amp: true
early_stopping_patience: 0
temp_lr_multiplier: 0.0
mixup_alpha: 0.2

# Temperature schedule
temp_schedule: "none"
temp_schedule_max: 20.0
temp_schedule_min: 10.0

# Group-aware sampling
group_aware_sampling: true
groups_per_batch: 64
hard_negative_ratio: 0.6
hard_negative_k: 10
class_weight_power: 0.5

# Preprocessing — none (raw collapsed embeddings)
preprocess_text_method: "none"
preprocess_cell_method: "none"

device: "cuda"
save_dir: "models/revision/zca_ablation/none"
