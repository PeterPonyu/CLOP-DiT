# Submission Packet Audit

**Date:** 2026-04-16

## Verdict

The current submission packet is **substantively ready** for upload after this cleanup pass.

## Verified products

- Revised manuscript: `revision/manuscripts/v2_revision/clop_dit_genes.tex`
- Revised manuscript PDF: `revision/manuscripts/v2_revision/clop_dit_genes.pdf` (37 pages)
- Marked manuscript source: `revision/manuscripts/diff/clop_dit_genes_tracked_changes.tex`
- Marked manuscript PDF: `revision/manuscripts/diff/clop_dit_genes_tracked_changes.pdf` (37 pages)
- Point-by-point response: `revision/response_letter/rebuttal_letter.tex`
- Point-by-point response PDF: `revision/response_letter/rebuttal_letter.pdf` (13 pages)
- Cover letter: `revision/response_letter/revision_cover_letter.tex`
- Cover letter PDF: `revision/response_letter/revision_cover_letter.pdf` (3 pages)

## Cleanup completed

- Removed internal workflow labels such as `Lane A/B/C/D` from the cover letter and rebuttal letter.
- Rewrote the top-level framing in the cover letter and rebuttal to sound more direct and less templated.
- Reconciled the `R3.1` story so the rebuttal summary no longer contradicts the main response.
- Removed the manuscript-side hidden revision patch block that exposed internal source paths and paste-ready notes.
- Removed the manuscript's explicit AI-tool disclosure block from the neutral submission copy.
- Generated the marked manuscript (`tracked_changes`) files, which were previously missing from `revision/manuscripts/diff/`.

## Verification performed

- `bash revision/response_letter/build.sh`
- `pdflatex -interaction=nonstopmode clop_dit_genes.tex` (two passes)
- `latexdiff <neutralized v1> revision/manuscripts/v2_revision/clop_dit_genes.tex`
- `pdflatex -interaction=nonstopmode clop_dit_genes.diff.tex` (two passes)
- `pdftotext` spot-checks on the response letter and manuscript PDFs for:
  - `Lane`
  - `Deferred`
  - `proof-of-concept`
  - `AI-assisted`
  - `Claude`
  - `Copilot`
  - `REVISION_PATCH`

## Remaining manual check

- Do one visual skim of the marked manuscript PDF before upload to confirm the latexdiff markup is readable and that no change bars or underlines obscure equations or figure captions.

## Residual note

- The manuscript still cites the public GitHub repository in the body/reference list. That is appropriate for a non-blinded submission, but should be re-checked if the target workflow changes to any blinded review format.

---

## Refreshed 2026-04-21

**Reconciliation against current state.**

1. **Manuscript rename.** `clop_dit_genes.{tex,pdf}` has been renamed to
   `clop_dit_manuscript.{tex,pdf}` (and the diff to `clop_dit_manuscript.diff.{tex,pdf}`).
   All "Verified products" paths above should be read with the new names. Page count
   is now 43 (manuscript) and diff PDF is 0 unresolved refs.

2. **Orphan-figure cleanup (US-ORPHAN).** Four PDFs had accumulated in
   `revision/manuscripts/v2_revision/figures/` that were never `\includegraphics`'d by
   `clop_dit_manuscript.tex`:

   - `enrichment_concordance.pdf` (22 KB) — stale output from `scripts/analysis/enrichment_analysis.py`
   - `fig03_composed.pdf` (81 KB) — alternative composed form generated by
     `src/visualization/article_composition.py`; manuscript uses individual panels fig03a/b/c
   - `fig08_composed.pdf` (1.3 MB) — same story for Fig 8; manuscript uses fig08a/b separately
   - `per_type_heatmap.pdf` (56 KB) — stale diagnostic from `scripts/analysis/per_type_dashboard.py`

   All four were untracked in git (ephemeral build outputs, not versioned source).
   All four have been **deleted** today to leave `v2_revision/figures/` containing
   exactly the 25 PDFs that the manuscript actually `\includegraphics`es. The source
   scripts remain and can regenerate these composed / diagnostic PDFs on demand.

3. **Figure-internal leakage fixed.** `figS_lane_a2_organism_stratified.pdf` previously
   baked the suptitle `"Organism-stratified generation quality (Lane A2)"` into the
   rendered PDF, leaking the internal workflow label "Lane A2" into both the clean
   manuscript and the diff. Fixed in `scripts/analysis/lane_a2_organism_boxplot.py`
   (suptitle stripped to `"Organism-stratified generation quality"`); a pre-existing
   `ax.ticklabel_format` call that crashed under the repo's default FuncFormatter was
   also removed (the explicit tick list on the next line already handles the formatting).
   Figure regenerated and propagated; token scan now shows **0 substantive leakage hits**
   across all 4 deliverables (see `LEAKAGE_REAUDIT.md` refresh section).

4. **Six figure polish fixes (2026-04-21).** `fig03c`, `fig05a`, `fig08b`, `fig09a`,
   `fig09b`, and `figS_lane_c_zero_shot` received targeted rendering fixes (label
   collisions, legend overflow, transparent bboxes). No caption text changed; no
   scientific claim changed. Manuscript PDF and diff PDF were rebuilt to embed the
   updated figures; `SUBMISSION_CHECKSUMS.txt` refreshed; `submission_bundle_v1.0.0.tar.gz`
   repacked (5 entries, 7.45 MB).

5. **Caption coverage (confirmed, not edited).** An audit raised concern about 5
   figures having "shallow" captions. Measurement over all 15 figure environments
   found every caption is between **545 and 1561 characters** of descriptive text
   (min on Fig 4 env, max on figS01). The audit had conflated `\includegraphics`
   line length (~37 chars) with `\caption{}` body length — a false positive. No
   caption edits were made.

**Current submission-packet verified products (post-refresh):**

- `revision/manuscripts/v2_revision/clop_dit_manuscript.{tex,pdf}` (43 pages,
   3.80 MB, 0 unresolved refs)
- `revision/manuscripts/diff/clop_dit_manuscript.diff.pdf` (3.85 MB,
   0 unresolved refs)
- `revision/response_letter/rebuttal_letter.pdf` (unchanged since 2026-04-16)
- `revision/response_letter/revision_cover_letter.pdf` (working-tree .tex edit is
   pre-existing; PDF unchanged since 2026-04-16)
- `revision/SUBMISSION_CHECKSUMS.txt` (refreshed, all 4 PDFs verify `OK`)
- `revision/submission_bundle_v1.0.0.tar.gz` (repacked 2026-04-21,
   5 entries, 7.45 MB)

**Refreshed verdict:** submission packet remains substantively ready for upload.
The 2026-04-16 residual note about the public GitHub URL still applies.

### Iteration 2 addendum (same day, 2026-04-21)

After the first refresh a user-flagged QA pass turned up three additional
items that have now been resolved:

6. **Fig A4 (`figS_lane_a2`) MW p-value / title collision.** The Mann--Whitney
   p-value annotation sat at axes-fraction `(0.97, 0.97)`, which at the
   compiled panel scale rendered visually on top of the subplot title text.
   Folded the p-value into the subplot title string itself (e.g.
   `"Centroid cosine  (MW p = 0.14)"`) — eliminates both the title-airspace
   collision and the data-region collision that an alternative "move down"
   attempt caused.

7. **Fig A3 (`figS_lane_a1`) large gap between figure body and caption.**
   The horizontal colorbar was centred in a generous bottom margin so the
   PDF retained an internal whitespace band below the cbar label. Rather
   than retune the figure layout (which regressed into xtick/cbar overlap
   during testing), the manuscript `\includegraphics` now uses
   `trim=0 60 0 0,clip` to crop the bottom band at include time; the
   stand-alone figure PDF is unchanged.

8. **Rebuttal letter figure coverage.** Previously only
   `figS_lane_c_zero_shot.pdf` was embedded inline in the rebuttal (for
   R3.1). Added inline `\includegraphics` previews for
   `figS_lane_a1_knn_family_heatmap` (R2.6 — KNN family confusion),
   `figS_lane_a2_organism_stratified` (R3.2 — species stratification), and
   `figS_lane_b3_forced_scarcity` (R3.3 — augmentation sweep). Rebuttal PDF
   grew from ~297 KB to ~420 KB, rebuilt cleanly with 0 unresolved refs.

All four submission PDFs were rebuilt after these changes;
`SUBMISSION_CHECKSUMS.txt` was re-generated and all four hashes verify
OK via `sha256sum -c`; `submission_bundle_v1.0.0.tar.gz` was repacked
(still 5 entries, 7.43 MB).

### 2026-04-24 font/layout addendum

The packet was refreshed again after a full figure-font and composed-page visual audit:

- Figure A1 panel E/D overlap fixed in the source figure layout.
- All article-facing figure PDFs regenerated and synced into both the clean manuscript and tracked-changes figure directories.
- Figure font baseline unified to Arial/Arial Bold; no DejaVu, STIX, Helvetica, or Type 3 figure-font fallback remains in `results/figures`, `revision/manuscripts/v2_revision/figures`, or `revision/manuscripts/diff/figures`.
- Clean manuscript PDF rebuilt: 45 pages, A4, no unresolved refs and no clean-log overfull/float warnings.
- Diff manuscript PDF rebuilt: 45 pages, A4, no unresolved refs; residual overfull boxes are latexdiff markup only.
- Rebuttal letter rebuilt: 19 pages.
- Cover letter rebuilt: 3 pages.
- `revision/SUBMISSION_CHECKSUMS.txt` refreshed to the 2026-04-24 deliverable hashes.

Current close-out report: `revision/figure_fix_reports/font_layout_logic_audit_2026-04-24.md`.
